
<div id='follow_form_<%= "#{@project.id}" %>'>
	<% if User.where(:id => @user.id)[0].relations.where(:project_id => @project.id)[0] != nil %>
		<%= render 'unfollow' %>
	<% else %>
		<%= render 'follow' %>
	<% end %>
</div>