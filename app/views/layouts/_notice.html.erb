<div class="container">
	<% if notice  == nil%>
	
	<% else %> 
	      <% notice.each do |key, value| %>
	        <%= content_tag(:div, "#{value}", :class => "alert alert-#{key}") %>
	      <% end %>
    <% end %>
</div>